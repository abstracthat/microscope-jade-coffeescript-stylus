template(name='layout')
  .container
    header.navbar.navbar-default(role='navigation')
      .navbar-header
        a.navbar-brand(href="{{pathFor 'postsList'}}") Microscope
    #main.row-fluid
      +yield
